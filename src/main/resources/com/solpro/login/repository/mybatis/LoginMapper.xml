<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.solpro.login.repository.mybatis.LoginMapper">

    <insert id="insert" parameterType="com.solpro.login.domain.LoginVO">
        insert into member(userId, userPassword, userName, userEmail, userPhone)
        values(#{userId}, #{userPassword}, #{userName}, #{userEmail}, #{userPhone})
    </insert>

</mapper>
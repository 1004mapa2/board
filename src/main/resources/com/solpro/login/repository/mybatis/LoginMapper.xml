<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.solpro.login.repository.mybatis.LoginMapper">

    <insert id="insert" parameterType="LoginVO">
        insert into member(userPassword, userName, userEmail, userPhone)
        values(#{userPassword}, #{userName}, #{userEmail}, #{userPhone})
    </insert>

</mapper>